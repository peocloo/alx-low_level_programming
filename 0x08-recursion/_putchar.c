#include <unistd.h>

/**
 * _putchar - writes character to stdout
 * @c: character argument
 *
 * Return: On success 1
 * on error -1, and errno is set appropriately
 */
int void(main)
{
	return (write(1, &c, 1));
}
